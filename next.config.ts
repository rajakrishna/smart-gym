import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['via.placeholder.com', 'via.placeholder.com', 'picsum.photos', 'loremflickr.com'],
  },
};

export default nextConfig;
