import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['via.placeholder.com', 'www.lorempixel.com', 'dummyimage.com', 'placekitten.com', 'placeimg.com', 'external-content.duckduckgo.com'],
  },
};

export default nextConfig;
