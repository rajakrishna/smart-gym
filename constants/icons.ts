import { LayoutDashboard, Home, Calendar, Coffee, MessageSquare, User } from 'lucide-react';

const ICONS = {
  layoutDashboard: LayoutDashboard,
  home: Home,
  calendar: Calendar,
  coffee: Coffee,
  messageSquare: MessageSquare,
  user: User,
} as const;

export default ICONS;
